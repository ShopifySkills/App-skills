# Evals for complete-makeup-look-bundle

Test prompts in `evals.json` are used to verify the skill produces:

1. **Eval 1** — No-discount "complete the look" bundle (foundation + sponge + setting spray): naming, PDP placement, one-click UX, metrics.
2. **Eval 2** — Discounted "Full Base Kit" (primer + concealer + powder + brush): bundle structure, copy, 10% set discount, metrics and margin.
3. **Eval 3** — Lip bundle (liner + lipstick + gloss) on WooCommerce: naming, placement, add-to-cart copy, metrics.

Run with skill-creator workflow: point the agent at this skill, execute each prompt, then grade outputs and optionally run description optimization.
